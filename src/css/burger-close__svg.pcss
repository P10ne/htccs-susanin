.burger-close__svg {
    width: 15px;
    height: 15px;
    fill: #fff;
    margin-right: 20px;

    @media (--media-mobile-size) {
        & {
            fill: var(--red-default-btn);
        }
    }
}
