.login_header {
    margin-right: 10px;
    fill: var(--header-gray-text);
    fill-rule: evenodd;
    width: 20px;
    height: 20px;

    &:hover {
        fill: #fff;
    }

    &:active {
        fill: var(--red-default-link)
    }
}
