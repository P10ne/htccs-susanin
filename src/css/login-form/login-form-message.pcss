.login-form-message {
    background-color: var(--red-default-btn);
    font-size: 18px;
    color: #fff;

    &__text {
        list-style: none;

        & li {
            padding: 5px 20px;
            &:first-of-type {
                padding-top:30px;
            }
            &:last-of-type {
                padding-bottom: 30px;
            }
        }

    }
}
