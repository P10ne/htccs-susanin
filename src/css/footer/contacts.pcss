.contacts {
    &__copyright {
        color: #fff;
        font-size: 14px;
        margin-bottom: 10px;
        display: inline-block;
    }

    &__text-item {
        color: var(--elements-gray);
        font-size: 14px;
        line-height: 20px;
        margin-bottom: 15px;
    }
}
