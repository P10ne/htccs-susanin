* {
    box-sizing: border-box;
    padding: 0;
    margin: 0;
}

body {
    color: var(--text-black);
}

button, input {
    border: none;
    background-color: rgba(0, 0, 0, 0);

    &:focus {
        outline: none;
    }
}
