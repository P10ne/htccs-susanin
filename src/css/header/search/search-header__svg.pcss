.search-header__svg {
    width: 31px;
    height: 31px;
    fill: var(--header-gray-text);

    &:hover {
        fill: #fff;
    }

    &:active {
        fill: var(--red-default-link);
    }

    @media (--media-tablet-size) {
        & {
            margin-right: 10px;
        }
    }
}
