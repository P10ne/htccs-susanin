.nav-link {
    font-size: var(--nav-items-font-size);
    color: #fff;
    text-decoration: none;

    &:hover {
        color: var(--red-default-link);
    }

    &_active {
        font-weight: bold;
    }
}
