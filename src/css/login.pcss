.login {
    display: flex;
    text-decoration: none;
    color: var(--header-gray-text);
    font-size: 18pt;
    align-items: center;

    &:hover {
        color: #fff;
    }

    &:hover svg {
        fill: #fff;
    }

    &-text {
        font-size: var(--header-font-size);
    }
}
